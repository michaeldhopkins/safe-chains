[[command]]
name = "exit"
description = "Terminates the current shell session with an optional exit code. The only side effect is ending the shell process; it cannot modify files or execute other commands. POSIX builtin; no independent release cycle."
url = "https://man7.org/linux/man-pages/man1/exit.1p.html"
level = "Inert"
bare = true
positional_style = true
