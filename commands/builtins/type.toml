[[command]]
name = "type"
description = "Reports how a command name would be interpreted by the shell (alias, function, builtin, file, or keyword). Read-only: prints classification information to stdout without executing anything or touching the filesystem. POSIX builtin; no independent release cycle."
url = "https://man7.org/linux/man-pages/man1/type.1p.html"
level = "Inert"
bare = false
standalone = ["--help", "--version", "-P", "-V", "-a", "-f", "-h", "-p", "-t"]
