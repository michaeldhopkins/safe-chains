[[command]]
name = "llvm-cxxfilt"
description = "Demangles C++ symbols (Itanium / Microsoft / Rust ABIs). Reads input, writes demangled output. Pure read."
url = "https://llvm.org/docs/CommandGuide/llvm-cxxfilt.html"
researched_version = "LLVM 18.x"
level = "SafeRead"
bare = true
positional_style = true
standalone = ["--help", "--version", "-?", "-h"]
