[[command]]
name = "llvm-symbolizer"
description = "LLVM — turns addresses into source locations / function names. Pure read."
url = "https://llvm.org/docs/CommandGuide/llvm-symbolizer.html"
researched_version = "LLVM 18.x"
level = "SafeRead"
bare = true
positional_style = true
standalone = ["--help", "--version", "-?", "-h"]
