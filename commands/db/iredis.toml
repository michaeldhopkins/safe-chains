[[command]]
name = "iredis"
description = "Interactive Redis REPL with autocompletion + syntax highlighting. Bare invocation drops into the REPL. Carve-out: --version / --help only."
url = "https://iredis.io/"
researched_version = "iredis 1.x"
level = "Inert"
bare = false
max_positional = 0
standalone = ["--help", "--version", "-h", "-V"]
