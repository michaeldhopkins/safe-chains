[[command]]
name = "sbcl"
description = "Steel Bank Common Lisp. Bare invocation drops into a REPL. --eval / --script / --load all execute Lisp code (arbitrary). Carve-out: --version / --help only."
url = "https://www.sbcl.org/"
researched_version = "SBCL 2.x"
level = "Inert"
bare = false
max_positional = 0
standalone = ["--help", "--version", "-h"]
