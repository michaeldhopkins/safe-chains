[[command]]
name = "ss"
description = "Displays detailed socket and connection information from the Linux kernel, replacing netstat. Strictly read-only: queries kernel socket tables via netlink and prints results to stdout. Does not modify network state, files, or generate network traffic. Faster than netstat on systems with many connections. The -p flag shows process information (may require root). Linux-only; the CLI surface is stable."
url = "https://man7.org/linux/man-pages/man8/ss.8.html"
level = "Inert"
bare = true
standalone = [
    "--all", "--dccp", "--extended", "--family",
    "--help", "--info", "--ipv4", "--ipv6", "--listening", "--memory",
    "--no-header", "--numeric", "--oneline", "--options",
    "--packet", "--processes", "--raw", "--resolve",
    "--sctp", "--summary", "--tcp", "--tipc", "--udp",
    "--unix", "--version", "--vsock",
    "-0", "-4", "-6", "-E", "-H", "-O", "-V",
    "-a", "-e", "-h", "-i", "-l", "-m", "-n", "-o", "-p", "-r", "-s", "-t", "-u", "-w", "-x",
]
valued = ["--filter", "--query", "-A", "-F", "-f"]
